# P15-v3 Closed Exact-Provenance Bonus Plan

Status: skeleton execution plan; no capture or product implementation is authorized.

## Success criteria

The recovery-v2 rebind skeleton succeeds when its validator and tests prove the single frozen Policy B (`0.00 -> 0.04`), all old evidence is immutable and protected, only the seven-file recovery-v2 seal directory is active, all predecessor fresh/rebind/supplemental/incident artifacts are archived and revoked, both recovery payloads remain unreleased, Click remains sealed, the run root remains absent, and `src/context_search_tool` is unchanged.

## Task 0A — Bind the P15-v2 terminal rejection

1. Bind reject index SHA-256 `cde8f5baf1aa8b6e96f04fdc24f221b450824563cb5721a59cf40872c3a69dd5`.
2. Recompute every identity, disposition, capture, comparison, terminal, and inventory digest listed by the index.
3. Require eight successful captures with eight distinct process/invocation identities, zero embedding/model requests, a rejected comparison, and a rejected terminal decision.
4. Treat Starlette, Requests, RedInk, and Daily as protected legacy characterization only, with zero v3 fresh efficacy credit.

Verify: the skeleton validator fails on any old-evidence mutation or missing artifact.

## Task 0B — Receive independent v3 inputs

1. Bind the recovery-v2 HTTPX and poetry-core public contracts, ciphertext/plaintext identity metadata, roster contract, seal-hash index, and Click carry-forward disposition to their exact reviewed SHA-256 values.
2. Accept only `.quality/p15-v3-recovery-seal-v2` and its exact seven-file inventory. Mark the old fresh directory, old manifest/harness rebind, release-byte supplemental, and incident disposition archived/revoked and reject them from active fields.
3. Validate public schema, strict compact canonical plaintext serialization, PBKDF2-HMAC-SHA256 parameters, public source commit/tree/license/inventory/content, per-slot and combined headroom, and opaque ciphertext `lstat` metadata. Do not read ciphertext bytes.
4. Read only the recovery-v2 Click disposition; do not open or hash any Click file. Keep all recovery plaintext/release artifacts absent and release/capture/Click open unauthorized.

Verify: any anchor drift, source/inventory drift, ciphertext mode/size drift, plaintext/release artifact, Click open record, or run-root creation makes validation fail.

## Task 0C — Build and review the product-free skeleton

1. Create the independent v3 design, this plan, a closed manifest, a read-only acceptance harness, and harness tests.
2. Encode only Policy B: direct-only closed exact tuples; candidate/same-chunk/context-overlap deterministic union and complete-tuple dedupe; the exact candidate `file_path`/`origin_chunk_ids` to target join; a full roster ordered after owner/ceiling/cohort by the existing ten-field total order; one derived winner; exact score part `exact_imported_symbol`; exact reason `exact imported symbol dependency`; binary `0.04`; and full-roster rerank with the same total order before context expansion/final selection.
3. Permanently reject Policy A and any arm other than `0.00` and `0.04`.
4. Freeze top-k, slots, budgets, caps, owner behavior, `0.85`, `0.8`, traversal, merge, and final-selection behavior.
5. Test a fixed full structural schema including selected files, every non-relation/producer field, every frozen relation bucket, module projection, and non-Python projection; cap equality; degree delta; Daily `44 -> 108` against 64 real v2 relation identities plus a real-edge position-65 relocation rejection; and derived full-roster runtime visibility.
6. Freeze a two-phase Task0 protocol with an independent middle gate. Phase A requires four separate-process, write-new, policy-0 raw-control captures per query (repeat 1/2 × canonical/reverse) with identical reconstructed runtime projections, then an immutable `0444` marker created by a fifth, identity-disjoint freezer process. Capture stops after those exact five files. An independent reviewer then reads their actual bytes and `lstat` metadata from the fixed control directory, verifies exact inventory, byte/canonical/projection digests, mode `0444`, no symlink, link count one, and PID/invocation isolation, and writes a mode-`0444`, write-new external control-seal disposition.
7. Keep Phase B unauthorized until a new manifest/harness revision binds the exact external-seal byte SHA-256. Its validator accepts only the fixed run-root path and that bound digest, rereads seal/control/marker/treatment bytes from disk, and derives the treatment result only from a seal-listed raw control. Add negatives for a coordinated rewrite of all controls, marker, treatment, seal, and internal digests; missing seal; wrong mode; and an extra control file. All must fail before treatment output is credited.

Verify: focused tests and `validate-skeleton` pass locally without creating evidence or modifying product code.

## Task 0D — Independent skeleton disposition

An independent reviewer checks this exact recovery-v2 manifest/harness rebind, revoked-chain closure, public-seal closure, Click boundary, formula completeness, zero-state, and product-code cleanliness. The current state is `awaiting_recovery_v2_rebind_review`; no predecessor disposition self-approves this revision.

Verify: a disposition must name and hash the exact reviewed files. A later change invalidates it.

## Task 1 — Conditional product implementation

This task is blocked until Task 0D and Task 0B approve a revised capture manifest. Then, and only then:

1. Add the minimum implementation needed to preserve the closed exact-provenance union through merge and expose one binary bonus at the frozen stage.
2. Do not change acquisition, edge generation, caps, slots, budgets, owner behavior, relation weight, decay, context expansion, or final selection.
3. Add focused unit/integration tests before evidence capture.

Verify: product tests pass; a source diff audit traces every changed line to Policy B.

## Task 2 — Conditional fresh single-factor ablation

Run baseline `0.00` and candidate `0.04` on the two independently selected fresh efficacy repositories using the approved online execution path. Local Ollama is forbidden. Freeze all non-factor inputs and capture process/request identity evidence.

Verify: the approved efficacy gates pass on both fresh repositories with no protected-regression loss and no unrelated structural difference.

## Task 3 — Conditional held-out Click evaluation

Only after the fresh efficacy gates pass and an independent Click disposition authorizes opening: verify the sealed digest, open once, run the frozen `0.00 -> 0.04` pair, and record the held-out decision. Do not tune after opening.

Verify: the held-out gates pass, the open record is auditable, and no unapproved arm or retry is present.

## Stop conditions

Fail closed immediately on policy drift, unknown structural differences, cap changes, Daily position above 64, missing runtime visibility, old-evidence mutation, recovery-v2 public-seal/source/inventory/headroom drift, any active reference to a revoked predecessor artifact, any recovery plaintext or release artifact, Click plaintext/open record before authorization, local Ollama use, model request during skeleton validation, pre-existing v3 run root, or any skeleton-phase `src/` change.
